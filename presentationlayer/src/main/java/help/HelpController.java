package help;

import base.Controller;

public class HelpController extends Controller<HelpModel, HelpView> {

    // ---- Constructor

    /**
     * Constructor.
     *
     * @param model the model.
     * @param view the view.
     */
    public  HelpController(HelpModel model, HelpView view) {
        super(model, view);
    }


    // ---- Methods

}
